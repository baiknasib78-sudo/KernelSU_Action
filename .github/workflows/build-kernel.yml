name: Build Kernel
on:
  workflow_dispatch:

jobs:
  build:
    name: Build Kernel by ${{ github.actor }}
    runs-on: ubuntu-22.04
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
    steps:
    - uses: actions/checkout@v4

    - name: Prepare Configuration
      run: |
        CONFIG_ENV=$(grep -w "CONFIG_ENV" config.env | head -n 1 | cut -d "=" -f 2)
        CONFIG_LIST=(
            KERNEL_SOURCE KERNEL_SOURCE_BRANCH KERNEL_CONFIG KERNEL_IMAGE_NAME
            ARCH ADD_LOCALVERSION_TO_FILENAME EXTRA_CMDS USE_CUSTOM_CLANG
            CUSTOM_CLANG_SOURCE CUSTOM_CLANG_BRANCH CUSTOM_CMDS CLANG_BRANCH
            CLANG_VERSION ENABLE_GCC_ARM64 ENABLE_GCC_ARM32 USE_CUSTOM_GCC_64
            CUSTOM_GCC_64_SOURCE CUSTOM_GCC_64_BRANCH CUSTOM_GCC_64_BIN
            USE_CUSTOM_GCC_32 CUSTOM_GCC_32_SOURCE CUSTOM_GCC_32_BRANCH
            CUSTOM_GCC_32_BIN ENABLE_KERNELSU KERNELSU_TAG ADD_KPROBES_CONFIG
            DISABLE-LTO ADD_OVERLAYFS_CONFIG DISABLE_CC_WERROR APPLY_KSU_PATCH
            USE_CUSTOM_ANYKERNEL3 CUSTOM_ANYKERNEL3_SOURCE CUSTOM_ANYKERNEL3_BRANCH
            ENABLE_CCACHE NEED_DTBO BUILD_BOOT_IMG SOURCE_BOOT_IMAGE
            KSU_EXPECTED_SIZE KSU_EXPECTED_HASH REMOVE_UNUSED_PACKAGES
        )

        for CONFIG in "${CONFIG_LIST[@]}"; do
            if [[ "$CONFIG" == "EXTRA_CMDS" || "$CONFIG" == "CUSTOM_CMDS" ]]; then
                echo "$CONFIG=$(grep -w "$CONFIG" "$CONFIG_ENV" | head -n 1 | cut -d ":" -f 2-)" >> $GITHUB_ENV
            else
                echo "$CONFIG=$(grep -w "$CONFIG" "$CONFIG_ENV" | head -n 1 | cut -d "=" -f 2-)" >> $GITHUB_ENV
            fi
        done

    - name: Remove unused packages
      if: env.REMOVE_UNUSED_PACKAGES == 'true'
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: true
        android: false
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: false

    - name: Set swap to 10G
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 10

    - name: Setup build kernel environment
      run: |
        echo "BUILD_TIME=$(TZ=Asia/Jakarta date "+%Y%m%d%H%M")" >> $GITHUB_ENV
        echo "DEVICE=$(echo ${{ env.KERNEL_CONFIG }} | sed 's!vendor/!!;s/_defconfig//;s/_user//;s/-perf//')" >> $GITHUB_ENV
        sudo apt-get update
        sudo apt-get install git ccache automake flex lzop bison gperf build-essential zip curl zlib1g-dev g++-multilib libxml2-utils bzip2 libbz2-dev libbz2-1.0 libghc-bzlib-dev squashfs-tools pngcrush schedtool dpkg-dev liblz4-tool make optipng maven libssl-dev pwgen libswitch-perl policycoreutils minicom libxml-sax-base-perl libxml-simple-perl bc libc6-dev-i386 lib32ncurses5-dev libx11-dev lib32z-dev libgl1-mesa-dev xsltproc unzip device-tree-compiler python2 python3 -y
        mkdir -p $GITHUB_WORKSPACE/kernel_workspace

    - name: Download Toolchains
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        
        # 1. Clang
        mkdir -p clang-aosp
        if [ "${{ env.USE_CUSTOM_CLANG }}" = "true" ]; then
            wget -O clang.bin "${{ env.CUSTOM_CLANG_SOURCE }}"
            tar -C clang-aosp/ -xf clang.bin || unzip clang.bin -d clang-aosp/
        else
            wget -q https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/${{ env.CLANG_BRANCH }}/clang-${{ env.CLANG_VERSION }}.tar.gz -O clang.tar.gz
            tar -C clang-aosp/ -zxf clang.tar.gz
        fi

        # 2. GCC 64
        mkdir -p gcc-64
        if [ "${{ env.ENABLE_GCC_ARM64 }}" = "true" ]; then
            if [ "${{ env.USE_CUSTOM_GCC_64 }}" = "true" ]; then
                wget -O gcc64.bin "${{ env.CUSTOM_GCC_64_SOURCE }}"
                tar -C gcc-64/ -xf gcc64.bin || unzip gcc64.bin -d gcc-64/
            else
                wget -q https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/+archive/refs/tags/android-12.1.0_r27.tar.gz -O gcc64.tar.gz
                tar -C gcc-64/ -zxf gcc64.tar.gz
            fi
        fi

        # 3. GCC 32
        if [ "${{ env.ENABLE_GCC_ARM32 }}" = "true" ]; then
            mkdir -p gcc-32
            if [ "${{ env.USE_CUSTOM_GCC_32 }}" = "true" ]; then
                wget -O gcc32.bin "${{ env.CUSTOM_GCC_32_SOURCE }}"
                tar -C gcc-32/ -xf gcc32.bin || unzip gcc32.bin -d gcc-32/
            else
                wget -q https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9/+archive/refs/tags/android-12.1.0_r27.tar.gz -O gcc32.tar.gz
                tar -C gcc-32/ -zxf gcc32.tar.gz
            fi
        fi

    - name: Download Kernel Source
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        git clone --recursive ${{ env.KERNEL_SOURCE }} -b ${{ env.KERNEL_SOURCE_BRANCH }} android-kernel --depth=1
        [ "${{ env.ADD_LOCALVERSION_TO_FILENAME }}" = "true" ] && echo "LOCALVERSION=$(cat android-kernel/localversion)" >> $GITHUB_ENV || echo "LOCALVERSION=" >> $GITHUB_ENV

    - name: Setup KernelSU Next
      if: env.ENABLE_KERNELSU == 'true'
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/android-kernel
        curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/dev/kernel/setup.sh" | bash -s dev
        if [ -d "KernelSU-Next" ]; then KSU_PATH="KernelSU-Next"; else KSU_PATH="drivers/kernelsu"; fi
        KSU_VERSION=$(cd $KSU_PATH && expr $(/usr/bin/git rev-list --count HEAD) + 10200)
        echo "UPLOADNAME=-KernelSU_Next_$KSU_VERSION" >> $GITHUB_ENV

    - name: Setup Configuration & Fixes
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/android-kernel
        
        [ -d "KernelSU-Next" ] && KSU_PATH="KernelSU-Next" || KSU_PATH="drivers/kernelsu"

        # Fixes sepele tapi krusial
        find $KSU_PATH -type f -exec grep -l "linux/compiler_types.h" {} + | xargs sed -i 's/linux\/compiler_types.h/linux\/compiler.h/g' || true
        find . -name "Makefile*" -o -name "Kbuild*" | xargs sed -i 's/-Werror//g' || true
        
        CONF="arch/${{ env.ARCH }}/configs/${{ env.KERNEL_CONFIG }}"
        sed -i '/CONFIG_CC_STACKPROTECTOR/d' $CONF
        echo "CONFIG_CC_STACKPROTECTOR_NONE=y" >> $CONF
        [ "${{ env.ENABLE_KERNELSU }}" = "true" ] && echo "CONFIG_KSU=y" >> $CONF

    - name: Setup ccache
      if: env.ENABLE_CCACHE == 'true'
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: build-kernel-${{ env.DEVICE }}
        max-size: 2G

    - name: Build Kernel
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/android-kernel
        
        CLANG_BIN=$(find $GITHUB_WORKSPACE/kernel_workspace/clang-aosp -type d -name "bin" | head -n 1)
        GCC64_BIN=$(find $GITHUB_WORKSPACE/kernel_workspace/gcc-64 -type d -name "bin" | head -n 1)
        GCC32_BIN=$(find $GITHUB_WORKSPACE/kernel_workspace/gcc-32 -type d -name "bin" | head -n 1)
        
        # Deteksi prefix secara dinamis
        PREFIX64=$(ls $GCC64_BIN | grep -m1 "aarch64.*-gcc$" | sed 's/gcc$//')
        PREFIX32=$(ls $GCC32_BIN | grep -m1 "arm.*-gcc$" | sed 's/gcc$//')

        MAKE_ARGS=(
            O=out
            ARCH=${{ env.ARCH }}
            CC="ccache clang"
            HOSTCC="gcc"
            HOSTCXX="g++"
            LD=ld.lld
            AR=llvm-ar
            NM=llvm-nm
            OBJCOPY=llvm-objcopy
            OBJDUMP=llvm-objdump
            STRIP=llvm-strip
            CROSS_COMPILE=$GCC64_BIN/$PREFIX64
            CROSS_COMPILE_ARM32=$GCC32_BIN/$PREFIX32
            KCFLAGS="-Wno-implicit-function-declaration -Wno-int-conversion -Wno-unknown-warning-option"
        )

        # Build dengan PATH Sandwich (Host tools pakai /usr/bin dulu)
        make "${MAKE_ARGS[@]}" ${{ env.KERNEL_CONFIG }} \
            PATH="/usr/bin:/bin:$CLANG_BIN:$GCC64_BIN:$GCC32_BIN"

        make -j$(nproc --all) "${MAKE_ARGS[@]}" \
            PATH="/usr/bin:/bin:$CLANG_BIN:$GCC64_BIN:$GCC32_BIN" \
            ${{ env.CUSTOM_CMDS }} \
            ${{ env.EXTRA_CMDS }}

    - name: Check Output & Prepare Artifacts
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        IMG="android-kernel/out/arch/${{ env.ARCH }}/boot/${{ env.KERNEL_IMAGE_NAME }}"
        if [ -f "$IMG" ]; then
            echo "CHECK_FILE_IS_OK=true" >> $GITHUB_ENV
            git clone https://github.com/osm0sis/AnyKernel3 --depth=1 AK3
            cp $IMG AK3/
            rm -rf AK3/.git*
        else
            echo "Kernel file not found!" && exit 1
        fi

    - name: Upload Results
      if: env.CHECK_FILE_IS_OK == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-${{ env.DEVICE }}-${{ env.BUILD_TIME }}
        path: kernel_workspace/AK3/*
